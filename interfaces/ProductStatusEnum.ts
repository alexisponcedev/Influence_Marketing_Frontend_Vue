export enum ProductSearchStatusEnum {
    all = 'all',
    // active = 'active',
    // inactive = 'inactive',
    hasPage = 'has-page',
    hasNoPage = 'has-no-page',
    hasSupport = 'has-support',
    hasNoSupport = 'has-no-support',
    hasAll = 'has-all'
}
