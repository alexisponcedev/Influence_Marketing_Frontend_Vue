export enum StructureType {
  String = 'string',
  Text = 'text',
  File = 'file',
  Image = 'image',
  Color = 'color',
  List = 'list',
  Url = 'url',
  Select = 'select',
  Object = 'object',
  idSelector = 'idSelector',
}
